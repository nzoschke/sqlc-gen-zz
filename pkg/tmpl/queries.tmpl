// Code generated by "sqlc-gen-zz". DO NOT EDIT.

package {{.Settings.Codegen.Out}}

import (
	"database/sql"
	"encoding/json"
	"time"

	"zombiezen.com/go/sqlite"

	{{overrideimports .Overrides}}
)

{{range .Queries}}
	{{if eq .Cmd ":exec"}}
		{{template "exec.tmpl" .}}
	{{else if eq .Cmd ":one"}}
		{{template "one.tmpl" .}}
	{{else if eq .Cmd ":many"}}
		{{if eq (len .Columns) 1}}
			{{template "many1.tmpl" .}}
		{{else }}
			{{template "many.tmpl" .}}
		{{end}}
	{{end}}
{{end}}

func jsonMarshal(v any) []byte {
	bs, _ := json.Marshal(v)
	return bs
}

{{range .Overrides}}
func jsonUnmarshal{{camel .GoType.Package}}{{.GoType.Type}}(bs []byte) {{.GoType.Package}}.{{.GoType.Type}} {
	var v {{.GoType.Package}}.{{.GoType.Type}}
	json.Unmarshal(bs, &v)
	return v
}
{{end}}


func timeParse(s string) time.Time {
	t, _ := time.Parse("2006-01-02 15:04:05", s)
	return t
}